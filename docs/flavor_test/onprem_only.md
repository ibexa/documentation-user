---
description: Verify that a page marked as on-premises only is built for the on-premises flavor and excluded from the SaaS flavor.
flavor: onprem
---

# On-premises only

This page uses `flavor: onprem`, so only the on-premises build contains it.

It's the mirror image of the SaaS-only page, and it confirms that filtering works in both
directions rather than only removing content from the SaaS flavor.

For the full contract, see the [flavor test hub](flavor_test.md), which both flavors
contain.
